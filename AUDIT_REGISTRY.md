# Реестр находок аудита (актуальный)

Компактный реестр по сквозному аудиту `khf-site-cms` + `khf-site-front`
(сессия 2026-09-14). Строки — только проверенное: «факт» воспроизведён,
«гипотеза» требует проверки в недоступном окружении. Статусы: **исправлено**
(с критерием приёмки и его подтверждением), **открыто** (владелец следующего
действия указан).

| ID | Приоритет | Статус | Файл/сценарий | Факт/гипотеза | Влияние | Исправление и приёмка |
|----|-----------|--------|---------------|---------------|---------|------------------------|
| A-1 | блокерный | исправлено | `khf-site-front/lib/api.ts` (`fetchAlerts`, `fetchAlertsActive`, `fetchRegions`) | Факт: сбой API возвращал `state:"calm"`/пустые списки — портал ЧС выдавал молчание бэкенда за подтверждённое отсутствие угроз | Ложное спокойствие на /alerts, /map, вспомогательных сводках | Возврат `null` при недоступности; страницы обстановки показывают «Данные об обстановке сейчас недоступны» (3 локали). Приёмка: e2e `graceful-degradation.spec.ts` (14 passed, вкл. /tj/alerts, /en/alerts); старые ожидания теста заменены |
| A-2 | высокий | открыто | ISR-старение данных | Гипотеза/продуктовое решение: срок допустимого устаревания предупреждений ( ISR 60 c) не утверждён; при отказе CMS посетитель видит последнюю закэшированную копию без метки времени | Гражданин может считать устаревшую обстановку текущей | Владелец: руководитель продукта КЧС. Условие закрытия: утверждённый SLA свежести + отображение «данные на HH:MM» на страницах обстановки |
| B-1 | высокий | исправлено | `khf-site-front/lib/api.ts` (`fetchAnnouncement`) | Факт: detail-страница объявления кэшировалась с тегом `cms`, который вебхук никогда не инвалидирует | Старая детальная страница объявления после публикации до следующего ISR-цикла | Использованы адресные теги `cms:announcements:*`. Приёмка: живая проверка — POST /api/revalidate с payload типа announcement → `revalidated:true`, повторный запрос страницы отдал `x-nextjs-cache: MISS` |
| B-2 | высокий | исправлено | `SITEMAP_TYPES` в `lib/cache-tags.ts` (front) и `app/Support/FrontendRevalidation.php` (CMS) | Факт: объявления отсутствовали в списке типов, инвалидирующих `cms:sitemap` | Новые/снятые объявления не выпадали из sitemap до 60-секундного ISR | `announcement` добавлен в оба списка синхронно (контракт payload проверяет точное совпадение тегов). Приёмка: pest 633 passed (SQLite и MySQL); в payload вебхука присутствует `cms:sitemap`; sitemap.xml собранного сайта содержит 72 URL объявлений |
| B-3 | средний | открыто | `fetchCategories`, `fetchLeadership`, `fetchStructureUnits` (front) | Факт: запросы тегированы `cms`, но вебхук этот тег не инвалидирует; у руководства/структуры/категорий нет событий ревалидации в CMS вообще | Правки руководства/структуры/категорий доходят до сайта только ISR-таймером (до 60 с) — приемлемо, но не мгновенно и не документировано как контракт | Владелец: backend. Условие закрытия: либо события `shell`-типа для этих справочников в `FrontendRevalidation`, либо явная фиксация «только ISR» в README (сейчас отмечено в README фронта) |
| C-1 | высокий | исправлено | `khf-site-front/app/[locale]/contacts/{ContactForm.tsx,content.ts}` | Факт: после POST форма обещала «Копия направлена на указанную почту», но в CMS нет ни отправки, ни очереди письма (`Api/SubmissionController.php` только сохраняет) | Недостоверное обещание гражданину; возможны обращения «куда делась копия» | Текст заменён на достоверное подтверждение с номером («Сохраните номер…»), 3 локали. Приёмка: e2e `contact-form.spec.ts` — обещание копии отсутствует, номер отображается |
| C-2 | высокий | исправлено | `ContactForm.tsx` | Факт: POST не передавал локаль — язык 422-ошибок зависел от настроек браузера | На /tj и /en ошибки валидации могли прийти на русском | Заголовок `Accept-Language: <api-locale>` (tj→tg). Приёмка: curl на реальную CMS — 422 на ru/tg/en в соответствующем языке; e2e на моке для 3 локалей |
| C-3 | средний | открыто | Копия обращения на email | Гипотеза (требование не подтверждено): отправка копии может быть нужна продуктово | — | Владелец: руководитель продукта. Условие закрытия: если требуется — очередь + повторы + наблюдаемость и различение «принято/в очереди/доставлено» |
| D-1 | средний | исправлено | `khf-site-front/lib/api.ts` | Факт: ни один запрос контента не имел application-таймаута | Зависший upstream держал SSR-рендер до системного TCP-таймаута | `AbortSignal.timeout(8000)` на всех GET (+15 c на POST формы). Осознанный размен: `signal` отключает мемоизацию запроса в пределах рендера (документация Next 16), не ISR. Приёмка: production build + e2e зелёные |
| D-2 | средний | исправлено | `ContactForm.tsx` | Факт: POST без таймаута и без защиты от двойной отправки | Вечный «Отправка…» при обрыве; повторное нажатие могло задвоить обращение | Таймаут 15 c; повтор инициирует только пользователь (кнопка блокируется на время запроса). Автоповтора нет |
| E-1 | высокий | открыто (сужено 2026-09-20) | CI обоих репозиториев | Факт: фронтовый e2e ходит против детерминированного мока `tests/fixtures/cms-server.mjs`; CMS Playwright в CI не запускается; сквозная связка «реальный Laravel + MySQL + очередь + собранный Next.js» нигде не проверяется автоматически. 2026-09-20 в CI CMS появилась матрица sqlite+mysql:8.4 (полный suite против MySQL) + `ops:production-check --skip-runtime` — закрыта СУБД-часть, но не сквозная | Интеграционные регрессии (контракт webhook, ревалидация, 2FA-поток) ловятся только вручную | Владелец: DevOps/backend. Условие закрытия: отдельный e2e-job: CMS (MySQL, queue worker, scheduler) + Next build/start + сценарии публикация/правка/снятие + настоящий тестовый 2FA (без отключения в приложении) |
| F-1 | средний | исправлено повторно (2026-09-20) | lock-файлы обоих репозиториев | Факт: pnpm-файлы удалены 2026-07-27, но вернулись: во фронте `pnpm-lock.yaml`/`pnpm-workspace.yaml` снова закоммичены (вернул коммит `b0bca54` 2026-07-28, правился `a83e1fd` 2026-09-14 — в день закрытия F-1), в CMS лежали untracked (созданы 2026-09-20). Кто-то локально работает pnpm-ом | Расхождение установки зависимостей и CI (`npm ci`) | Выполнено 2026-09-20: во фронте `git rm` оба файла, паттерны в `.gitignore` обоих репо, pnpm-файлы удалены с диска, node_modules обоих репо переустановлены `npm ci` (front: typecheck + 210 unit зелёные; CMS: typecheck + vite build зелёные). Приёмка регресса: если pnpm появится снова — см. комментарий в `.gitignore` фронта; менеджер меняем только целиком (CI+deploy вместе) |
| F-2 | средний | исправлено | `khf-site-front/vitest.config.{ts,mts}` | Факт: два конфига с разными include/environment; реальным был `.ts` | Неоднозначность discovery тестов | `vitest.config.mts` удалён. Приёмка: `npm test` — 186 passed, .mjs- и jsdom-тесты обнаруживаются |
| F-3 | средний | исправлено | `khf-site-front/README.md` | Факт: README описывал общий тег `cms`, `revalidateTag("cms","max")` — в коде адресные теги и `{expire:0}` | Документация вводила в заблуждение при отладке кэша | README переписан под фактический контракт тегов |
| F-4 | низкий | исправлено | CMS Prettier | Факт: 7 файлов в `resources/` закоммичены неформатированными — `format:check` в CI падал бы | Красный CI | `prettier --write` по 7 файлам; `format:check` чист |
| G-2 | средний | артефакт готов (2026-09-20); установка на прод — открыто | `php artisan queue:work` локально | Факт: воркер по умолчанию слушает только очередь `default`; heartbeat идёт в `critical` — readiness (`/api/v1/ready`) остаётся 503 при «здоровом» но простаивающем воркере. Прод-топология задокументирована в `DEPLOYMENT.md` §2.8 (4 воркера, все именованные очереди), `php artisan dev` поднимает все очереди — риск остаётся при ручном `queue:work` без `--queue=` | Ложный not_ready; сборка фронта (cms-readiness) блокируется | `deploy/supervisor/khf-cms-workers.conf` теперь артефакт репо (2026-09-20). Владельцу: установить на прод-хост, `supervisorctl reread && update` |
| G-1 | средний | локальная часть закрыта (2026-09-20); подтверждение на проде — открыто | `khf-site-cms/storage/logs/laravel.log` | Факт (обновлено 2026-09-20): лог 183 МБ и растётся, 30 834 повтора `read error on connection to lerd-redis`; локальный `.env` до сих пор `LOG_CHANNEL=stack`/`LOG_STACK=single` (в `.env.example` уже daily); сам флап lerd-redis не устранён | Заполнение диска выводит из строя весь сервис, не только логирование | Выполнено 2026-09-20: локальный `.env` переведён на `LOG_CHANNEL=daily`. Владельцу: на проде подтвердить daily-канал + алерт заполнения диска; разобраться со стабильностью lerd-redis (30к+ ошибок/день) |
| H-1 | средний | исправлено | `khf-site-front/app/globals.css` (`slideFade`) | Факт: fade-анимация слайдера (opacity 0→1) давала полупрозрачный текст в момент сканирования — axe фиксировал контраст 2–2.9:1 на /en (флейк-падения e2e) | Нестабильные a11y-тесты; формально WCAG-нарушение в момент анимации | Анимация заменена на transform-сдвиг без изменения прозрачности. Приёмка: accessibility-набор 21 passed × 4 прогона подряд |
| I-1 | **блокерный** (пересмотрен 2026-09-20) | исправлено на стороне приложения (2026-09-20); нагрузочная проверка на стенде — открыто | `config/trustedproxy.php` (новый), `app/Providers/AppServiceProvider.php:282-291`, `routes/api.php:79-81`, `deploy/nginx/cms.conf` | Факт (проверено 2026-09-20): настройка trusted proxies в проекте **отсутствовала вовсе** (grep по `trustProxies` пуст) — формулировка «trusted proxies уже настроены» в прошлой сессии была ошибкой. Все per-IP лимиты бьются по IP ближайшего хопа: `submissions` 10/мин при SSR-проксировании Next.js = 10 обращений в минуту на всех граждан суммарно; в `ip_address` обращения писался IP прокси | Ложный 429 на всю аудиторию при всплеске; юридически неверный IP заявителя | Выполнено 2026-09-20: `config/trustedproxy.php` (env `TRUSTED_PROXIES`: `*`/`REMOTE_ADDR`/список подсетей; читается глобальным middleware), `.env.example` + прод-шаблон DEPLOYMENT §2.2 + локальный `.env` (`REMOTE_ADDR`); nginx-артефакт передаёт `X-Forwarded-For/$proxy_add_x_forwarded_for` + Proto/Port. Приёмка: 4 новых теста `TrustedProxiesTest` (доверенный хоп резолвит IP/scheme, подделанный префикс цепочки игнорируется, недоверенный пир не читает заголовки, пустой конфиг = прежнее поведение), полный suite 715 passed. Осталось: на прод-.env указать реальные подсети LB/Next и прогнать нагрузочную проверку лимитов |

## Матрица проверок (2026-09-14, локальный стенд: PHP 8.5.10, Node 22.23.2, MySQL 8.4.11, Next 16.2.10, Laravel 13.20)

| Команда/сценарий | Окружение | Результат |
|---|---|---|
| `npm run typecheck`, `lint`, `api:types:check` | front, Node 22 | ✅ чисто |
| `npm test` (vitest) | front | ✅ 186 passed / 31 файл |
| `npm run build` | front + реальная CMS (lerd https) | ✅ (после поднятия queue worker) |
| `npm run test:e2e` (оба проекта, вкл. backend-down) | front + mock CMS | ✅ 115 passed, 2 skipped |
| `npm run test:a11y` × 4 | front | ✅ 21 passed стабильно |
| `npm run lighthouse` | front + реальная CMS | ✅ perf≥99/a11y=100 на 4 страницах (первый холодный прогон home=85 — прогрев ISR/TLS) |
| `npm run bundle:report` | front | ✅ в бюджете (567.6 KiB max route) |
| Вебхук ревалидации объявления | front build + реальная CMS | ✅ `revalidated:true` → страница MISS |
| 422-локализация обращений (ru/tg/en) | реальная CMS | ✅ сообщения на языке локали |
| `composer ci:check` (pint, eslint, prettier, tsc, node tests) | CMS | ✅ после фикса F-4 |
| `composer types:check` (PHPStan) | CMS | ✅ 0 ошибок |
| `php artisan test` | CMS, SQLite :memory: | ✅ 633 passed, 1 skipped |
| `php artisan test` (DB_* MySQL) | CMS, MySQL 8.4.11 `khf_cms_test_ci` (создана и удалена) | ✅ 633 passed, 1 skipped |
| CMS Playwright (браузерные CMS-сценарии) | — | ❌ не запускался в этой сессии (E-1) |
| Real-CMS e2e публикации/снятия через UI | — | ❌ не запускался (E-1) |
| Backup/restore drill, production-check, деплой | — | ❌ вне локального стенда, требует стенд и разрешение |

## Реестр находок аудита 2026-09-20 (сквозной: CMS + front + DevOps)

Новая сессия перед продакшеном. Статусы до начала работ этой сессии; исправленное в этой сессии помечено датой в столбце статуса.

| ID | Приоритет | Статус | Файл/сценарий | Факт | Влияние | Исправление и приёмка |
|----|-----------|--------|---------------|------|---------|------------------------|
| J-1 | блокерный | исправлено (2026-09-20) | `khf-site-cms/.env.bkp.20260728-121247` (коммит `03d0fd4`, лежит на `origin/main` публичного репо `aminjon1065/khf-site-cms`) | Факт: файл с реальным `APP_KEY` и `FRONTEND_REVALIDATION_SECRET` закоммичен и запушен в публичный GitHub; `.gitignore` не покрывает паттерн `.env.bkp.*`. Текущие значения в `.env` от утёкших отличаются (проверено сравнением), но старые действительны там, где их не сменили | Подделка сессионных cookies/расшифровка `two_factor_secret` среды, где ключ ещё актуален; вектор атаки на гос-ресурс | Выполнено 2026-09-20: `git rm` + история переписана filter-branch (refs/original удалены, reflog expire, gc --prune=now), force-push `main` и dependabot-ветки (в `local/optimize-run` и `work/plan` файла не было); `.gitignore` → `.env*`+`!.env.example`; локальный `.env` уже содержит ротированные значения (сверено с `.env.local` фронта). Приёмка: `git log --all --full-history -- .env.bkp.*` пуст. Осталось владельцу: GitHub может отдавать недостижимые коммиты по прямому SHA до GC — при желании запросить gc у поддержки; пароль почты из бэкапа сменить, если он где-то актуален |
| J-2 | блокерный | исправлено (2026-09-20) | front `.github/workflows/ci.yml:24,29` против `scripts/build.mjs:26` | Факт: CI задаёт `CMS_BUILD_MODE: production` + `NEXT_PUBLIC_SITE_URL: http://127.0.0.1:3000` без `SITE_URL_ALLOW_LOCAL=1`; guard `assertSiteUrlForBuild` обязан уронить шаг Build. Дополнительно при разборе выяснилось: шаг Type-check падал раньше — `next-env.d.ts` не трекается git, а tsc запускается до первого `next build`, поэтому без ambient-объявлений `next/image-types/global` все импорты картинок дают TS2307 (CI красный с 17.09) | Валидированный артефакт собрать в CI невозможно | Выполнено 2026-09-20: `scripts/ensure-next-env.mjs` создаёт каноничный `next-env.d.ts` при отсутствии (чинит и CI, и свежий клон), `typecheck` вызывает его первым; `SITE_URL_ALLOW_LOCAL: "1"` добавлен в env джобы. Приёмка: локально на симуляции свежего клона typecheck чист; зелёный run CI после пуша |
| J-3 | блокерный | исправлено в репо (2026-09-20); первый деплой на стенд — открыто | оба репо: отсутствие деплой-артефактов | Факт: DEPLOYMENT.md описывает прод полноценно, но в репо нет ни systemd-юнита, ни supervisor-конфига, ни deploy-скрипта, ни Dockerfile — вся инфраструктура существует как проза (в доке уже были расхождения: `php8.3-fpm.sock` при PHP 8.5; фронту §3.1 велит `.env.local`, §4 деплоит `shared/.env.production`; `systemctl reload khf-front` на юните без ExecReload) | Первый прод-деплой — ручная сборка по инструкции; ошибки копипасты ничем не ловятся | Выполнено 2026-09-20: каталог `deploy/` в CMS (supervisor на 4 воркера, cron.d, systemd-юнит фронта, nginx cms+front с X-Forwarded-For/Proto, deploy-cms/deploy-front/rollback с DRY_RUN и чисткой релизов; shellcheck чист), расхождения доки исправлены. Приёмка первой части: файлы в репо. Условие закрытия до конца: первый деплой на стенд выполнен этими скриптами |
| J-4 | высокий | открыто | `app/Http/Middleware/RequireTwoFactor.php:42` + `database/seeders/UserSeeder.php:18,76-99` | Факт: `require_2fa` по умолчанию `false`; `db:seed` без прода-гарда создаёт Superadmin `admin@khf.tj` с паролем `password` и `is_active=true` | Один лишний `--force` на проде = бэкдор с известным паролем без 2FA | `abort_if(app()->isProduction())` в UserSeeder; в прод-чеклисте включать `require_2fa=true` до открытия доступа |
| J-5 | высокий | открыто | `app/Providers/AppServiceProvider.php:203`, OPS_RUNBOOK | Факт: «алерт» = `Log::critical`, канала доставки нет; внешний uptime-мониторинг не настроен нигде | Сбой узнают от граждан, а не из мониторинга | Внешний uptime на `/api/v1/health`,`/ready`,`khf.tj` + доставка critical (email/TG). Приёмка: тестовый алерт доходит |
| J-6 | высокий | открыто | front `app/api/vitals/route.ts:45-91` | Факт: публичный POST без rate-limit транслирует каждый payload в CMS (fetch до 2 c) | Фронт как усилитель нагрузки на CMS | In-memory лимит по IP (60/мин). Приёмка: нагрузочный тест не валит CMS через фронт |
| J-7 | высокий | открыто | front `next.config.ts` (нет `poweredByHeader: false`), `tests/e2e/security-headers.spec.ts:4-11` | Факт: `next start` шлёт `X-Powered-By: Next.js`; контракт-тест заголовок не проверяет | Раскрытие стека для гос-сайта, регрессия незамеченной | `poweredByHeader: false` + assertion в spec |
| J-8 | средний | открыто | front `lib/api.ts:750` + `lib/cache-tags.ts:113-136` | Факт: тег `cms:regions:${locale}` и голый `cms` не порождаются `buildRevalidationTags` — контакты приёмной ревалидируются только 60-с ISR (README документирует это для categories/leadership/structure, но не regions) | Устаревшие контакты гос-приёмной до минуты | Включить `cms:regions:*` в контракт тегов обеих сторон синхронно |
| J-9 | средний | частично (2026-09-20) | оба репо, git-гигиена | Факт: 36 коммитов с сообщением «0.0.1», тегов и релизов нет | Откат «на прошлый релиз» по git невозможен; bisect бессмыслен | Сессия 2026-09-20 переведена на conventional commits; теги и защита main от прямых пушей — осталось владельцу |
| J-10 | низкий | открыто | CMS `.env.example` (`SESSION_SECURE_COOKIE`), `public/robots.txt`, `config/fortify.php:117-119` | Факт: (а) `SESSION_SECURE_COOKIE` нет в примере при требовании `true` в DEPLOYMENT.md; (б) robots CMS-хоста разрешает индексацию всего; (в) password-reset без throttle | Cookie без Secure на проде; индексация админки; email-бомбинг | Добавить переменную в пример; `Disallow: /`; limiter на reset+тест |
