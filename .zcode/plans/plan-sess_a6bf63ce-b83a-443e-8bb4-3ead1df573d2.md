Контекст: CMS уже полностью реализована (аутентификация + 2FA, все модули контента, редакционный процесс, медиа-библиотека, публичный API v1 с OpenAPI-контрактом, роли, тесты). План — точечные улучшения уровня продакшен, без ломки существующего.

## 1. Редирект с главной (маленькое, но явное требование)
- `routes/web.php:36`: заменить `redirect('/dashboard')` на логику: гость → `route('login')`, авторизованный → `/dashboard`. Реализовать через `Auth::check()` в замыкании маршрута.
- Обновить/добавить тест: гость получает redirect на `/login`, авторизованный пользователь — на `/dashboard`.

## 2. Быстрое создание контента (главное улучшение удобства)
- Добавить в TopBar (или на дашборд) кнопку «+ Создать» — выпадающее меню с прямыми ссылками на формы создания: новость, проект, объявление, предупреждение, документ, инструкция, страница (только модули, доступные по правам пользователя; права уже приходят в shared props через `HandleInertiaRequests`).
- На дашборд добавить блок «Быстрые действия» под приветствием (кнопки «Новая новость», «Новый проект» и т.д. по правам).

## 3. Полировка форм новостей и проектов
- Пройтись по `resources/js/pages/news/form.tsx` и `projects/form.tsx` вместе с `EditorialFormShell`: проверить последовательность полей, обязательность, подсказки, поведение черновика vs публикации, автосохранение, горячие клавиши сохранения (хук `save-shortcut` уже есть).
- Исправить найденные шероховатости: непоследовательные подписи, отсутствие placeholder'ов/подсказок, пустые состояния, длинные сценарии без индикации прогресса.

## 4. UX-аудит вживую и исправления
- Поднять приложение (`composer run dev` / `php artisan serve`), зайти под сеяным пользователем, пройтись по ключевым экранам (логин → дашборд → список новостей → форма → публикация → медиа) через браузер.
- Зафиксировать и исправить фактические неудобства: навигация, сайдбар, скорости, сообщения об ошибках, пустые состояния списков.
- Дашборд и навигация: при аудитe проверить группировку сайдбара, командную палитру (cmdk), добавить недостающие точки входа (например, «Корзина», «Очередь переводов» уже есть — проверить их заметность).

## 5. Проверки
- `vendor/bin/pint --dirty --format agent` после PHP-правок.
- Обновить затронутые Pest-тесты + новые тесты на редирект `/`; прогон `php artisan test --compact` по затронутенным файлам.
- `npm run build` для проверки фронтенда.

Не входит в объём: API-клиент (Sanctum/токены), изменения схемы БД, новый функционал модулей — только удобство и полировка.